// Package applicationattempt owns the durable local lifecycle of an automatic
// vacancy-application dispatch attempt. It deliberately knows nothing about
// HH transport, candidate facts, or application read models.
package applicationattempt
