// Package candidateinterpretation interprets one Candidate clarification
// answer into an untrusted acquisition value. It owns the prompt,
// structured-output parsing, and semantic retry boundary, but cannot mutate or
// persist Candidate knowledge.
package candidateinterpretation
