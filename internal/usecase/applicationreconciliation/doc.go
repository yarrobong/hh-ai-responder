// Package applicationreconciliation proves an existing HH vacancy response
// using bounded, read-only provider evidence. It never owns or receives an HH
// write capability and never releases an unresolved automatic attempt.
package applicationreconciliation
