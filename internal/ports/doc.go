// Package ports contains consumer-driven persistence capabilities.
//
// The package is intentionally limited to contracts required by current
// orchestration flows. It owns no persistence DTOs and has no dependency on
// the root package, JSON, PostgreSQL, HH, AI, or dashboard code.
package ports
